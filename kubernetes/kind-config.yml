kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
- role: control-plane
- role: worker
- role: worker

# this will set up a Kind cluster with one control-plane (master node) and two worker nodes
 
# create the cluster with this command: kind create cluster --name [clusterName] --config kind-config.yml
# verify the created cluster - kubectl cluster-info
